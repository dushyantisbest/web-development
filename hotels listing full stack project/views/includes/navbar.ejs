<nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top shadow-sm">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="/listing">
      <i class="fa-solid fa-compass text-danger me-2"></i>
      <span class="fw-semibold">Flatmate</span>
    </a>

    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link active px-3" href="/listing">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-3" href="/listing/add">Add Listing</a>
        </li>

        <% if (!currUser) { %>
        <li class="nav-item">
          <a href="/user/login" class="nav-link px-3">Login</a>
        </li>
        <li class="nav-item">
          <a href="/user/signup" class="btn btn-danger rounded-pill px-4 ms-2"
            >Sign up</a
          >
        </li>
        <% } %> <% if (currUser) { %>
        <li class="nav-item">
          <form action="/user/logout" method="post">
            <button
              type="submit"
              class="btn btn-danger rounded-pill px-4 ms-2 logout-btn"
            >
              Logout
            </button>
          </form>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<!-- Add margin to body to prevent content from hiding under fixed navbar -->
<!-- <style>
  body {
    padding-top: 4rem;
  }

  @media (max-width: 768px) {
    .navbar .btn {
      margin: 0.5rem 0;
      display: block;
      width: 100%;
    }

    .navbar-nav {
      padding: 1rem 0;
    }
  }
</style> -->
